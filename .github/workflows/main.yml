on: [push]

jobs:
  Testing-stuffings:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/saraidery/fortran-tensor-library-docker-action:main
    steps:
      - run: mkdir build
      - run: ls /__w/fortran-tensor-library/fortran-tensor-library/build
      - run: pwd
      - run: ls
