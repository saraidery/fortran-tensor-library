on: [push]

jobs:
  Testing-stuffings:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/saraidery/fortran-tensor-library-docker-action:main
    steps:
      - run: git clone --recursive git@github.com:saraidery/fortran-tensor-library.git
      - run: cd fortran-tensor-library
