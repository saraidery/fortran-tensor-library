on: [push]

jobs:
  Testing-stuffings:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/saraidery/fortran-tensor-library-docker-action:main
    steps:
      - run: mkdir build
      - run: cd build
      - run: pwd
